# def d(n, m):
#     return n % m == 0
import re

# def d(n, m):
#     return n % m == 0
# def f(x, a):
#     return (a < 50) and ((not d(x, a)) <= (d(x, 10) <= (not d(x,12))))
#
# print(max(a for a in range(1,100) if all(f(x, a) for x in range(1, 100))))

# def d(n, m):
#     return n % m == 0
# def f(x, a):
#     return ((x & 52 !=0) and (x & 36 == 0)) <= (not(x & a == 0))
#
# print(min(a for a in range(1,100) if all(f(x, a) for x in range(1,1000))))

# def f(x, y, a):
#     return (x >= 11) or (3*x < y) or (x*y < a)
# print(min(a for a in range(1000) if all(f(x, y, a) for x in range(0,100) for y in range(0,100))))
















